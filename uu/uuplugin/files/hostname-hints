# 主机名特征表 —— OUI 认不出来时，凭 DHCP 主机名认游戏机
#
# 每行一条 ERE（扩展正则），大小写不敏感；# 开头是注释，空行忽略。
# 各行会被拼成一条交替式（a|b|c），所以行内如果要用 | 必须自己加括号：
# 写成 "a|b" 会拼出 "...|a|b|..."，那是两条规则，不是你想的一条。
# 行首行尾多余的 | 会被自动削掉；行内写出 "a||b" 这种空分支则整表停用并
# 报警告——空分支匹配空串，也就是匹配所有主机名，那等于把全网设备都标成
# 候选（autoadd=1 时会直接把它们全从 PassWall 里拽出来）。
#
# 语法合法却匹配一切的写法（"."、".*"、"^"、"$"、"[a-z0-9]"）现在也拦得住：
# 脚本拿两串哨兵（一串纯字母、一串纯数字）去试，两串都命中才判为通配，同样
# 整表停用并告警。只命中一串的（如 "[a-z]"、"[0-9]"）算"宽"不算"通配"，照常
# 放行——宽到什么程度由你自己负责，脚本只挡"匹配一切"。
#
# 想加自家设备：直接在文件末尾追加一行即可，不用改脚本、不用重装包。
# 想彻底关掉主机名判定：把本文件清空（留注释也行），OUI 判定不受影响。
# 本文件是 conffiles，升级包不会覆盖你的改动（代价是新版规则也不会自动进来）。

# Nintendo Switch 的默认主机名形如 "Nintendo-Switch" / "switch"
switch
nintendo
# 开发机 / 部分固件用 NX 代号；两侧限定非字母数字，免得 "linux"、"nxlog" 误伤
(^|[^a-z0-9])nx([^a-z0-9]|$)
