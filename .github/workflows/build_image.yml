name: build_image
on: push

jobs:
  build_passwall:
    name: Build Passwall
    runs-on: ubuntu-latest
#    container: 
#      image: openwrtorg/sdk:x86_64-master
    steps:
    - uses: actions/checkout@v3
    - name: Print Pwd
      run: |
        pwd; ls -alh
  